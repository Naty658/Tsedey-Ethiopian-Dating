workflows:
  ios-workflow:
    name: iOS Workflow
    max_build_duration: 120
    environment:
      vars:
        NODE_ENV: production
    scripts:
      - npm ci
      - npx expo export --platform ios
    artifacts:
      - dist/**